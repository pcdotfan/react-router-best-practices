name: React Router Best Practices CI

on:
  push:
    branches: [main]
    paths:
      - 'skills/react-router-best-practices/**'
      - 'packages/react-router-best-practices/**'
  pull_request:
    branches: [main]
    paths:
      - 'skills/react-router-best-practices/**'
      - 'packages/react-router-best-practices/**'

jobs:
  validate:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/react-router-best-practices
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v2
        with:
          version: 10.24.0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
          cache-dependency-path: packages/react-router-best-practices/pnpm-lock.yaml
      - run: pnpm install
      - run: pnpm validate
      - run: pnpm build
